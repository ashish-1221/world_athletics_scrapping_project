{
    "name": "World Athletics - Scrapy Devcontainer",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "ms-python.vscode-pylance",
                "ms-azuretools.vscode-docker"
            ]
        }
    },
    "settings": {
        "python.defaultInterpreterPath": "/usr/local/bin/python"
    },
    "postCreateCommand": "bash -lc 'if [ -f requirements.txt ]; then pip3 install --no-cache-dir -r requirements.txt; else pip3 install --no-cache-dir scrapy; fi && pip3 install --no-cache-dir playwright && python -m playwright install chromium'",
    "remoteUser": "vscode"
}